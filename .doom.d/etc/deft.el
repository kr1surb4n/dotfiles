;;; ../.dotfiles/.doom.d/etc/deft.el -*- lexical-binding: t; -*-

;;; Deft config
(setq deft-extensions '("txt" "tex" "org"))
(setq deft-directory "~/docs")
(setq deft-recursive t)
(setq deft-use-filename-as-title t)
